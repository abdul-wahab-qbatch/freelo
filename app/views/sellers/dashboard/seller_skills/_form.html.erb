<%= form_with(model: seller_skill) do |form| %>
  <% if seller_skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(seller_skill.errors.count, "error") %> prohibited this service from being saved:</h2>

      <ul>
        <% seller_skill.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
